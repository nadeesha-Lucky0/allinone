{
  "name": "web-wedding-plans",
  "version": "1.0.0",
  "description": "Root workspace to run both backend and frontend",
  "scripts": {
    "dev": "concurrently --kill-others -n \"BACKEND,FRONTEND\" -c \"blue,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run install:all && npm run build --prefix frontend",
    "start": "node server.js",
    "postinstall": "npm install --prefix backend",
    "download-videos": "node download_videos.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
